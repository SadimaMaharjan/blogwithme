{{#if postData.length}}
<section>
    <h2>Your Posts</h2>
    {{#each postData as |post|}}
    {{> dashboard-posts }}
    {{/each}}
</section>
{{/if}}

<button type="submit" class="btn"><a class="link" href="/dashboard/new">+ New Post</a></button>